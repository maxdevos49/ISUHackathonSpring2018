function render() {
	//game.debug.inputInfo(16,16);

	//game.debug.geom(floor,'#0fffff');
	//game.debug.body(player);
  
	 //for (var i = 0; i < newPotion.length; i++){
	 	//game.debug.body(newPotion[i]);
	 //}

	//game.debug.body(player.sword);

}